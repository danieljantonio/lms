import { NextPage } from 'next';

const CLasses: NextPage = () => {
	return <div>Classes page, contain the list of classes taught along with the stats of each class.</div>;
};

export default CLasses;
